const client = require("@lot-api-server/client");
const { CREATE_TOKEN_PATH, SECRET_KEY } = require("@lot-api-server/config");

module.exports = function createToken() {
    return client.post(CREATE_TOKEN_PATH, {
        secret_key: SECRET_KEY
    })
}