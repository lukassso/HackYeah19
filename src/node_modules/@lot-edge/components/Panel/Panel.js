import React from 'react'

import './Panel.css'

export default function Panel({ children, background, color, onMouseEnter, className }) {
    const style = { background, color }
    const cn = `panel ${className}`
    return <div className={cn} style={style} onMouseEnter={onMouseEnter}>
        {children}
    </div>
}